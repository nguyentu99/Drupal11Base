import {
  FontFamily,
  FontSize,
  FontColor,
  FontBackgroundColor,
} from 'ckeditor5/src/font';

/**
 * Font plugins are exposed under the `font` UMD namespace (font.FontFamily, etc.).
 */
export default {
  FontFamily,
  FontSize,
  FontColor,
  FontBackgroundColor,
};
