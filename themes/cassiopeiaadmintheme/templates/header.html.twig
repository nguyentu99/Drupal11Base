<nav class="app-header navbar navbar-expand bg-body">
  <div class="container-fluid">
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link" data-lte-toggle="sidebar" href="#" role="button" aria-label="{{ 'Toggle sidebar'|t }}">
          <i class="bi bi-list" aria-hidden="true"></i>
        </a>
      </li>
      <li class="nav-item d-none d-md-block">
        <a href="{{ path('<front>') }}" class="nav-link">{{ 'Home'|t }}</a>
      </li>
    </ul>

    <ul class="navbar-nav ms-auto align-items-center">
      <li class="nav-item">
        <a class="nav-link" href="#" data-lte-toggle="fullscreen" aria-label="{{ 'Toggle fullscreen'|t }}">
          <i data-lte-icon="maximize" class="bi bi-arrows-fullscreen" aria-hidden="true"></i>
          <i data-lte-icon="minimize" class="bi bi-fullscreen-exit" style="display: none;" aria-hidden="true"></i>
        </a>
      </li>

      {% if account_menu %}
        {{ account_menu }}
      {% elseif logged_in %}
        <li class="nav-item dropdown user-menu">
          <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown" role="button" aria-expanded="false">
            <img src="{{ base_path }}{{ directory }}/images/user2-160x160.jpg" class="user-image rounded-circle shadow" alt="">
            <span class="d-none d-md-inline">{{ current_user_display_name }}</span>
          </a>
        </li>
      {% endif %}
    </ul>
  </div>
</nav>
